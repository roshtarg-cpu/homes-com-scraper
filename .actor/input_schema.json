{
  "schemaVersion": 1,
  "title": "Homes.com Scraper Input",
  "type": "object",
  "properties": {
    "searchQuery": {
      "title": "Search Query",
      "type": "string",
      "description": "Location to search (city, state, ZIP code, or address)",
      "default": "San Francisco, CA",
      "prefill": "Austin, TX",
      "editor": "textfield"
    },
    "maxResults": {
      "title": "Max Results",
      "type": "integer",
      "description": "Maximum number of properties to scrape",
      "default": 50,
      "prefill": 10,
      "minimum": 1,
      "maximum": 1000
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Residential proxies recommended for best results",
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"]
      },
      "editor": "proxy"
    }
  },
  "required": ["searchQuery"]
}
